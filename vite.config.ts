// Lovable's TanStack configuration already provides the required
// TanStack Start, React, Tailwind, TypeScript paths and Nitro setup.
import { defineConfig } from "@lovable.dev/vite-tanstack-config";

export default defineConfig({});
